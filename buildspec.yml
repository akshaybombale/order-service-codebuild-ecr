version: 0.2

phases:
  install:
      runtime-versions:
       java: corretto11
  build:
    commands:
      - echo Entered the build phase
      - mvn package
    
  post_build:
    commands:
      - echo Build completed  successfully on `date`
      
      
artifacts:
  files:
  - target/order-service.jar
  discard-paths: yes 

